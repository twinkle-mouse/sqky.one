---
import Navigation from "$components/Navigation.astro";
import Page, { type Props as PageProps } from "$layouts/CommonPage.astro";
import { siteName } from "$lib/writings";

type Props = Omit<PageProps, "siteName">;
---

<Page {...Astro.props} siteName={siteName}>
    <Fragment slot="head-tags">
        <link rel="alternate" type="application/rss+xml" title={siteName} href={new URL("rss.xml", import.meta.env.SITE)} />
    </Fragment>

    <Navigation slot="nav">
        <slot name="nav:page-links" slot="nav:page-links" />
        <slot name="nav:other-categories" slot="nav:other-categories" />
        <slot name="nav:footer" slot="nav:footer" />
    </Navigation>

    <slot name="page-title" slot="page-title" />
    <slot />
</Page>
